{
  "scripts": {
    "start": "run-p watch serve",
    "sass": "sass index.scss:index.css",
    "watch": "npm run sass -- --watch",
    "serve": "live-server"
  },
  "devDependencies": {
    "prettier": "2.7.1"
  }
}
